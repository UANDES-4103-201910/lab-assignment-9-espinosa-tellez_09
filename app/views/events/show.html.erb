<p id="notice"><%= notice %></p>

<p><%= @event[:name] %></p>
<p><%= @event[:start_date] %></p>
<p><%= @place[:name] %></p>

<%= form_with model: UserTicket.new do |form| %>
	Tickets:<br>
	<%= form.collection_select(:ticket, Ticket.where(event_id: @event[:id]), :id, :description) %>
	<%= form.number_field :number_of_tickets %><br>
	<br>
	<div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%= link_to 'Edit', edit_event_path(@event) %> |
<%= link_to 'Back', events_path %>
